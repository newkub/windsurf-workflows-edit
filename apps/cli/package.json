{
  "name": "cli",
  "private": true,
  "type": "module",
  "bin": {
    "windsurf-workflows": "./dist/index.js"
  },
  "scripts": {
    "build": "tsdown src/index.ts --format esm --out-dir dist",
    "dev": "bun run src/index.ts",
    "lint": "biome lint --write",
    "format": "biome format --write",
    "test": "vitest run",
    "verify": "bun run build"
  },
  "dependencies": {
    "@clack/prompts": "^0.11.0",
    "gray-matter": "^4.0.3",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.10",
    "@types/node": "^24.0.0",
    "@types/js-yaml": "^4.0.9",
    "tsdown": "^0.18.3",
    "vitest": "^4.0.16"
  }
}
