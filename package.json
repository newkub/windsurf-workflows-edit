{
  "name": "windsurf-workflows-edit",
  "type": "module",
  "private": false,
  "bin": {
    "windsurf-workflows": "./dist/cli.js"
  },
  "scripts": {
    "build": "nuxt build",
    "build:cli": "tsdown cli.ts --dts",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "bun link && nuxt prepare",
    "lint": "biome lint --write",
    "format": "biome format --write"
  },
  "dependencies": {
    "nuxt": "^4.2.2",
    "vue": "^3.5.25",
    "vue-router": "^4.6.4"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.10",
    "@unocss/nuxt": "^66.5.10",
    "@vue/test-utils": "^2.4.6",
    "nuxt-vitest": "^0.11.5",
    "tsdown": "^0.18.1",
    "vitest": "^4.0.16"
  }
}
